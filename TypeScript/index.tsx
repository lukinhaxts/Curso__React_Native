const bool : boolean = true;
const numero : number = 200;
const texto : string = "Lucas"

const alunos : string [] = ["Julio", "Flávia", "Lucas", "Pedro Henrique", "Maria Clara"]

const alunoNota : [string, number] = ["Lucas", 10]

function mult(arg1 : number, arg2 : number) : void {

    console.log(arg1*arg2)
}

function multi(arg1 : number, arg2 : number) : number {

    console.log(arg1*arg2)

    return arg1*arg2
}